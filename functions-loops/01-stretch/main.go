package main

/*
  Resources: http://www.golang-book.com/8/index.htm
             http://www.golang-book.com/9/index.htm

  TODO: write a function Move which takes in a type position struct for an argument.
  Assume you are passing a struct which contains integers x, y, dx, and dy. Have the function
  return two values which represent x + dx and y + dy.

  TODO: write a METHOD called RateChange whose receiver is a pointer to a Position struct. The method
  should take in two integer arguments, the first which mutates dx on the Position struct by addition. The
  second integer mutates dy on the Position struct by addition. Return the struct

*/
